# v1.0.9

Pure Hive was removed from this blank-bot repo. This release is trading-only again (Autokeys / Discord / panel unchanged).

## Upgrade

```bash
git pull
npm install
npm run build
pm2 restart tf2bot
```

Or `!updaterepo`.
